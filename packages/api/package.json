{
  "name": "@hive/api",
  "version": "0.1.0",
  "description": "HIVE query API — semantic search + Gemini synthesis",
  "type": "module",
  "scripts": {
    "start": "node --env-file=../../.env --import tsx/esm src/api_server.ts",
    "dev": "node --env-file=../../.env --import tsx/esm --watch src/api_server.ts"
  },
  "dependencies": {
    "@hive/agent": "*",
    "@hive/core": "*",
    "@fastify/cors": "^9",
    "@fastify/static": "^7",
    "fastify": "^4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
